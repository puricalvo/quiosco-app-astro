---
import Logo from "@/assets/logo.svg";
import Layout from "./Layout.astro";
import AdminSidebar from "@/components/admin/AdminSidebar";
import LogoutButton from "@/components/ui/LogoutButton.astro";
interface Props {
  title: string;
}
const { title } = Astro.props;
---
<Layout title={`Panel de Administración: ${title}`}>
  <div class="md:flex">
    <aside
      class="md:w-[320px] md:h-screen bg-white border-r border-gray-200 flex flex-col p-5"
    >
      <div class="flex justify-center">
        <Logo />
      </div>
      <AdminSidebar client:only="react" />
      <LogoutButton />
    </aside>
    <main class="md:flex-1 md:h-screen md:overflow-y-scroll p-5">
      <h1 class="text-2xl my-10">{title}</h1>
      <slot />
    </main>
  </div>
</Layout>